# sensitive data has been trimmed

set sidebar_width=30
set sidebar_visible=yes

color sidebar_new blue default

macro index,pager A "<pipe-message>abook --add-email-quiet<return>" "add the sender address to abook"
bind index \CP sidebar-prev
bind index \CN sidebar-next
bind index \CO sidebar-open
bind pager \CP sidebar-prev
bind pager \CN sidebar-next
bind pager \CO sidebar-open

macro index b '<enter-command>toggle sidebar_visible<enter>'
macro pager b '<enter-command>toggle sidebar_visible<enter>'
macro pager <Up> '<exit><previous-undeleted>'
macro pager <Down> '<exit><next-undeleted>'
macro index V <collapse-all>
set pager_stop=yes
set quit=ask-no

set mbox_type=maildir
set folder=<folder>
set spoolfile=+"INBOX"
set move=no
# .hcache the directory
set header_cache=~/.mutt/.hcache

set tilde
set markers
set sig_dashes

folder-hook . "exec collapse-all"
#message-hook . "exec skip-quoted"
#folder-hook =Search "l ~N<enter>"
folder-hook =Search "set delete=yes"

set smart_wrap 
set pager_context=5 
set fast_reply=yes
set mark_old=no


set ispell="aspell -e -c"
macro generic ,f "<shell-escape>mairix " "search via mairix"
set pager_index_lines=15
set realname="<name>"
set from="<email id>"
set sendmail="/usr/bin/msmtp"
set envelope_from=yes
set use_from=yes


set record=+Sent
set postponed=+Drafts
set edit_headers=yes
set signature=~/.mutt/.signature

set alias_file=~/.mutt/aliases
source ~/.mutt/aliases
set editor="vim +/^$ ++1"
set delete=no
set sort_browser=reverse-date
set sort=threads

#bind index ] previous-line
bind pager ] previous-line
#set dsn_notify=”failure,delay”
#set dsn_return="hdrs"
set sort_aux=reverse-last-date-received
set reverse_name=yes

set text_flowed
auto_view text/html
alternative_order text/html text/plain
set display_filter="<fiter>"
set signature=~/.signature
set include=yes

mailboxes "<mailboxes>"


color attachment brightmagenta default  
color error brightwhite red   # errors yell at you in red
color hdrdefault red default   # headers
color indicator brightyellow magenta # currently selected message
color markers brightcyan default  # the + for wrapped pager lines
color message brightcyan default  # informational messages, not mail
color normal default default    # plain text
color quoted green default  # quoted text
color search brightgreen default # hilite search patterns in the pager
color signature red default    # signature (after "-- ") is red 
color status brightyellow blue # status bar is yellow *on blue*   
color tilde blue default  # ~'s after message body
color tree red default    # thread tree in index menu is magenta
color signature brightred default
color underline yellow default
color header cyan default ^(From|Subject|To): # Important headers
color body magenta default "(ftp|http)://[^ ]+"  # picks up URLs
color body magenta default [-a-z_0-9.]+@[-a-z_0-9.]+ 

#  Coloring quoted text - coloring the first 7 levels:
color quoted    cyan default
color quoted1   yellow default
color quoted2   red default
color quoted3   green default
color quoted4   cyan default
color quoted5   yellow default
color quoted6   red default
color quoted7   green default

#  Colorize smileys:  :-)  ;-)  :-/  :-(
color body  yellow default "[;:]-[)/(|]"
color body  yellow default "[;:][)/(|]"
color body  green default "[[:alpha:]]\+://[^ ]*"

color index brightyellow default ~N      # New
color index yellow default ~O              # Old
color index magenta default ~F
color index blue default ~T
color index red default ~D



set confirmappend=no

macro index \Ci "<copy-message>=Important<enter>"
macro pager \Ci "<copy-message>=Important<enter>"

macro index \Ct "<copy-message>=Todo<enter>"
macro pager \Ct "<copy-message>=Todo<enter>"

#macro pager .c "T.\n;WN^T.\n" "mark all messages as read"
#macro index .c "T.\n;WN^T.\n" "mark all messages as read"
